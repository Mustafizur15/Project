#include<stdio.h>

int main()
{
    int x;
    int y;
    int z;
    printf("Enter two numbers:");
    scanf("%d%d",&x,&y);
    z=x+y;
    z=x-y;
    z=x*y;
    z=x/y;
    z=x%y;

    printf("The sum is=%d",z);
    printf("The sub is=%d",z);
    printf("The mul is=%d",z);
    printf("The div is=%d",z);
    printf("The mud is=%d",z);
return 0;
}
